//hook for changing buttons on dash. 
//balls, strikes, foul, hit(?maybe)
import {useState} from 'react';

export const useDashUpdate = (key, initialValue) => {
const [values, setValues] = useState(initialValue); //set state to initialValue

const 



}